# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#
# For a list of all available plugins, check out
#
#     https://docs.fastlane.tools/plugins/available-plugins
#

# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

android.useAndroidX=true
android.enableJetifier=true

# Override any GradleException that AAPT2 should make non-blocking.
android.nonTransitiveRClass=false

# Gradle
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Java version configuration
java.sourceCompatibility=JavaVersion.VERSION_17
java.targetCompatibility=JavaVersion.VERSION_17

# Set Java 17 as source and target
org.gradle.java.home=C:\\Program Files\\Microsoft\\jdk-17.0.15.6-hotspot
